---
phase: 01-vertical-slice
plan: 08
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/components/PrivacyBanner/PrivacyBanner.tsx
  - src/components/PrivacyBanner/PrivacyBanner.module.css
  - src/components/DropZone/DropZone.tsx
  - src/components/DropZone/DropZone.module.css
  - src/components/DropZone/DropZone.test.tsx
  - src/index.css
autonomous: true
requirements:
  - INGEST-01

must_haves:
  truths:
    - "PrivacyBanner renders the exact UI-SPEC copy and is sticky at top"
    - "DropZone accepts only PDF files (application/pdf)"
    - "Drop or click triggers onFileAccepted with the File"
    - "Wrong-type drop triggers onFileRejected with reason 'wrong-type'"
    - "DropZone has correct ARIA: role=button, aria-label, tabIndex=0"
    - "Idle/dragging/processing/complete state copy matches UI-SPEC verbatim"
  artifacts:
    - path: "src/components/PrivacyBanner/PrivacyBanner.tsx"
      provides: "Sticky privacy banner component"
      exports: ["PrivacyBanner"]
    - path: "src/components/DropZone/DropZone.tsx"
      provides: "Accessible drag-drop file picker for PDF input"
      exports: ["DropZone", "DropZoneProps", "DropZoneState"]
    - path: "src/components/DropZone/DropZone.test.tsx"
      provides: "RTL tests for accept/reject paths and accessibility"
  key_links:
    - from: "src/components/DropZone/DropZone.tsx"
      to: "react-dropzone"
      via: "useDropzone hook"
      pattern: "useDropzone"
    - from: "src/index.css"
      to: "design tokens"
      via: "CSS custom properties added per UI-SPEC"
      pattern: "--space-md|--flag-red"
---

<objective>
Build the two top-of-page UI primitives: `PrivacyBanner` (sticky banner with the locked privacy copy) and `DropZone` (react-dropzone v15 wrapper with idle/dragging/processing/complete states). Also extend `src/index.css` with the spacing scale tokens and the new flag color tokens declared in UI-SPEC.md.

Purpose: Satisfies INGEST-01 ("User can drag-drop a hospital bill PDF"). PrivacyBanner sets the privacy frame above-the-fold per CONTEXT.md decision. The DropZone is the user's first interaction.
Output: Two components + DropZone tests + extended CSS token set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-vertical-slice/01-CONTEXT.md
@.planning/phases/01-vertical-slice/01-RESEARCH.md
@.planning/phases/01-vertical-slice/01-UI-SPEC.md

<interfaces>
DropZoneProps contract:
  onFileAccepted(file: File): void
  onFileRejected(reason: 'wrong-type' | 'too-large' | 'unknown'): void
  state: 'idle' | 'dragging' | 'processing' | 'complete'

PrivacyBannerProps: none — fully static.

react-dropzone v15 API:
  useDropzone({ accept, multiple, maxSize, onDropAccepted, onDropRejected, disabled, noClick }) returns { getRootProps, getInputProps, isDragActive }
  FileRejection type: { file: File; errors: { code, message }[] }
  Error codes: 'file-invalid-type' | 'file-too-large' | 'too-many-files' | 'file-too-small'

UI-SPEC tokens to add to src/index.css (only ones not already present from Phase 0):
  --space-xs: 4px; --space-sm: 8px; --space-md: 16px; --space-lg: 24px; --space-xl: 32px; --space-2xl: 48px; --space-3xl: 64px
  --surface: maps to existing --code-bg in light mode
  --flag-red: #dc2626 (light), #7f1d1d (dark)
  --flag-red-text: #ffffff (light), #fecaca (dark)
  --flag-red-border: #b91c1c (light), #991b1b (dark)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend src/index.css with UI-SPEC tokens</name>
  <files>src/index.css</files>
  <read_first>
    - /Users/kevinrichards/Projects/hightimized/src/index.css
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 32-130, Spacing + Color sections)
  </read_first>
  <action>
Read the current `src/index.css` first (Phase 0 already wrote `--text`, `--text-h`, `--bg`, `--border`, `--code-bg`, `--accent`, `--accent-bg` plus font stacks per UI-SPEC line 619 "Pre-Populated From").

Append (or merge into the existing `:root` block) the spacing scale + flag color tokens missing from Phase 0. In the dark-mode `@media (prefers-color-scheme: dark)` block, also add the dark flag tokens.

Light mode (in :root):
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
  --space-2xl: 48px;
  --space-3xl: 64px;
  --surface: var(--code-bg);
  --flag-red: #dc2626;
  --flag-red-text: #ffffff;
  --flag-red-border: #b91c1c;

Dark mode (inside the existing dark-mode media query):
  --surface: #1f2028;
  --flag-red: #7f1d1d;
  --flag-red-text: #fecaca;
  --flag-red-border: #991b1b;

Also add a global keyframes block usable by all components:

  @keyframes spin { to { transform: rotate(360deg); } }
  @keyframes badgeEntrance { from { opacity: 0; transform: scale(0.85); } to { opacity: 1; transform: scale(1); } }
  @keyframes toastIn { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }
  @keyframes toastOut { to { opacity: 0; } }

And the reduced-motion override per UI-SPEC line 575:
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      transition-duration: 0.01ms !important;
    }
  }

Do not remove any existing tokens, fonts, or rules. Append-only.
  </action>
  <verify>
    <automated>grep -E -- '--space-md: 16px' src/index.css && grep -E -- '--flag-red: #dc2626' src/index.css && grep '@keyframes spin' src/index.css && grep 'prefers-reduced-motion' src/index.css && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -- '--space-xs:' src/index.css` returns a match
    - `grep -- '--space-3xl:' src/index.css` returns a match
    - `grep -- '--flag-red:' src/index.css` returns a match
    - `grep -- '--flag-red-text:' src/index.css` returns a match
    - `grep '@keyframes spin' src/index.css` returns a match
    - `grep '@keyframes badgeEntrance' src/index.css` returns a match
    - `grep 'prefers-reduced-motion' src/index.css` returns a match
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Design tokens and shared keyframes available globally for all Phase 1 components.</done>
</task>

<task type="auto">
  <name>Task 2: PrivacyBanner component</name>
  <files>src/components/PrivacyBanner/PrivacyBanner.tsx, src/components/PrivacyBanner/PrivacyBanner.module.css</files>
  <read_first>
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 140-159, "1. PrivacyBanner")
  </read_first>
  <action>
Create `src/components/PrivacyBanner/PrivacyBanner.tsx`:
- React component, no props.
- Renders a `<div>` with `role="banner"`, className from the CSS module `banner` class.
- Single child: the exact text from UI-SPEC line 160:
  `Your data never leaves your device. No upload, no account.`
- Imports the CSS module.

Create `src/components/PrivacyBanner/PrivacyBanner.module.css`:
- `.banner` rule with: `position: sticky`, `top: 0`, `z-index: 100`, `width: 100%`, `height: 40px`, `background: var(--surface)`, `border-bottom: 1px solid var(--border)`, `display: flex`, `align-items: center`, `justify-content: center`, `padding: 0 var(--space-md)`, `font-size: 16px`, `color: var(--text)`.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/components/PrivacyBanner/PrivacyBanner.tsx`
    - File exists: `src/components/PrivacyBanner/PrivacyBanner.module.css`
    - `grep "Your data never leaves your device. No upload, no account." src/components/PrivacyBanner/PrivacyBanner.tsx` returns a match
    - `grep 'role="banner"' src/components/PrivacyBanner/PrivacyBanner.tsx` returns a match
    - `grep "position: sticky" src/components/PrivacyBanner/PrivacyBanner.module.css` returns a match
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>PrivacyBanner renders the locked copy as a sticky element with correct ARIA role.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DropZone component + tests</name>
  <files>src/components/DropZone/DropZone.tsx, src/components/DropZone/DropZone.module.css, src/components/DropZone/DropZone.test.tsx</files>
  <read_first>
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 162-216, "2. DropZone")
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 472-481, DropZone copy contract)
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-RESEARCH.md (lines 280-340, "Question 3: react-dropzone v15")
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-RESEARCH.md (lines 848-887, "DropZone Test Pattern")
  </read_first>
  <behavior>
    - Test 1 ("calls onFileAccepted with the dropped PDF file"): drop a single application/pdf File; assert onFileAccepted called with that file, onFileRejected not called.
    - Test 2 ("calls onFileRejected with 'wrong-type' for image/jpeg"): drop an image/jpeg File; assert onFileRejected called with 'wrong-type', onFileAccepted not called.
    - Test 3 ("idle state copy matches UI-SPEC"): renders heading text "Drop your hospital bill here." and subtitle "PDF only for now. We'll do the math."
    - Test 4 ("processing state copy"): when state='processing', renders "Reading your bill…" and "This runs in your browser. Nothing is uploaded."
    - Test 5 ("complete state copy"): when state='complete', renders "Drop another bill to start over."
    - Test 6 ("aria attributes"): root element has role="button", tabIndex=0, aria-label containing "Drop your hospital bill here".
  </behavior>
  <action>
Create `src/components/DropZone/DropZone.tsx`. The component:
- Imports `useDropzone`, `FileRejection` from `react-dropzone`.
- Imports the CSS module.
- Exports `type DropZoneState = 'idle' | 'dragging' | 'processing' | 'complete'`.
- Exports `interface DropZoneProps`:
    onFileAccepted: (file: File) => void
    onFileRejected: (reason: 'wrong-type' | 'too-large' | 'unknown') => void
    state: DropZoneState
- Exports `function DropZone(props: DropZoneProps): JSX.Element`.

Implementation:
- Call `useDropzone({ accept: { 'application/pdf': ['.pdf'] }, multiple: false, maxSize: 50 * 1024 * 1024, onDropAccepted: (files) => props.onFileAccepted(files[0]), onDropRejected: (rejections) => mapErrorCode, disabled: state === 'processing' || state === 'complete', noClick: state === 'complete' })`.
- mapErrorCode: read `rejections[0]?.errors[0]?.code` and call onFileRejected with 'wrong-type' if 'file-invalid-type', 'too-large' if 'file-too-large', else 'unknown'.
- Render the root `<div>` with `getRootProps()` spread. Force ARIA on root: `role="button"`, `tabIndex={0}`, `aria-label="Drop your hospital bill here, or press Enter to browse"`, `aria-describedby="dropzone-subtitle"`. (react-dropzone forwards getRootProps which already sets some — explicit override is fine.)
- Inside root: hidden `<input {...getInputProps()} aria-hidden="true" />`.
- Conditional content per state:
  - idle / dragging: `<h2>Drop your hospital bill here.</h2>` (or in dragging state, `<h2>Drop it.</h2>` with subtitle hidden); subtitle `<p id="dropzone-subtitle">PDF only for now. We'll do the math.</p>`.
  - processing: spinner `<div className={styles.spinner} aria-hidden="true" />`; `<h2>Reading your bill…</h2>`; `<p>This runs in your browser. Nothing is uploaded.</p>`.
  - complete: small text `<p>Drop another bill to start over.</p>`.
- The `dragging` UI variant kicks in when `isDragActive` (returned by useDropzone) is true while state==='idle'. Apply class `styles.dragging` conditionally.

CSS module rules (vanilla CSS modules per CONTEXT.md decision):
- `.zone`: rounded rectangle, `border: 2px dashed var(--border)`, `border-radius: 12px`, `background: var(--bg)`, `min-height: 180px`, `width: 100%`, `max-width: 672px`, `margin: 0 auto`, `display: flex`, `flex-direction: column`, `align-items: center`, `justify-content: center`, `gap: var(--space-sm)`, `padding: var(--space-md)`, `cursor: pointer`, `transition: border-color 150ms ease, background-color 150ms ease`.
- `.zone:focus-visible`: `outline: 2px solid var(--accent)`, `outline-offset: 2px`.
- `@media (min-width: 640px) { .zone { min-height: 240px; } }`.
- `.dragging`: `border: 2px solid var(--accent)`, `background: var(--accent-bg)`.
- `.heading`: `font-size: 28px`, `font-weight: 700`, `color: var(--text-h)`, `line-height: 1.2`, `margin: 0`, `text-align: center`.
- `.subtitle`: `font-size: 14px`, `font-weight: 400`, `color: var(--text)`, `line-height: 1.4`, `margin: 0`, `text-align: center`.
- `.complete`: `min-height: 48px`, `border: 1px solid var(--border)`, `background: var(--surface)`, `font-size: 16px`, `color: var(--text)`.
- `.spinner`: `width: 24px`, `height: 24px`, `border: 3px solid var(--border)`, `border-top-color: var(--accent)`, `border-radius: 50%`, `animation: spin 0.8s linear infinite`.

Create `src/components/DropZone/DropZone.test.tsx` per RESEARCH.md Q10 lines 848-887:
- Import `render`, `screen`, `fireEvent` from `@testing-library/react`; `vi`, `describe`, `test`, `expect` from `vitest`; the DropZone component.
- Helper `mockDropEvent(files)` returning `{ dataTransfer: { files, items: files.map(f => ({ kind: 'file', type: f.type, getAsFile: () => f })), types: ['Files'] } }`.
- Each test renders `<DropZone state="idle" onFileAccepted={...} onFileRejected={...} />` with vi.fn callbacks.
- Test 1: drop a `new File(['%PDF-1.4'], 'bill.pdf', { type: 'application/pdf' })`. Assert onFileAccepted called once with the file. (Note: react-dropzone reads the file's type — the magic bytes don't matter; only the MIME type does.)
- Test 2: drop a `new File(['data'], 'bill.jpg', { type: 'image/jpeg' })`. Assert onFileRejected called with `'wrong-type'`.
- Test 3: assert `screen.getByRole('heading', { level: 2, name: /Drop your hospital bill here/i })` and `screen.getByText("PDF only for now. We'll do the math.")` are present.
- Test 4: rerender with state="processing"; assert heading text "Reading your bill…" and subtitle "This runs in your browser. Nothing is uploaded." are present.
- Test 5: rerender with state="complete"; assert "Drop another bill to start over." is present.
- Test 6: `expect(screen.getByRole('button')).toHaveAttribute('aria-label', expect.stringContaining('Drop your hospital bill here'))`.

For Test 1, react-dropzone's accept-by-MIME logic in jsdom requires the dataTransfer mock to expose `items` with `getAsFile`. The pattern is documented in RESEARCH.md Q10 line 887 (verified via the cited GitHub discussion).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test:run src/components/DropZone/DropZone.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/components/DropZone/DropZone.tsx`
    - File exists: `src/components/DropZone/DropZone.module.css`
    - File exists: `src/components/DropZone/DropZone.test.tsx`
    - `grep "useDropzone" src/components/DropZone/DropZone.tsx` returns a match
    - `grep "application/pdf" src/components/DropZone/DropZone.tsx` returns a match
    - `grep "Drop your hospital bill here" src/components/DropZone/DropZone.tsx` returns a match
    - `grep "PDF only for now" src/components/DropZone/DropZone.tsx` returns a match
    - `grep "Reading your bill" src/components/DropZone/DropZone.tsx` returns a match
    - All 6 tests pass under `pnpm test:run`
  </acceptance_criteria>
  <done>DropZone is fully accessible, accepts only PDFs, renders all four UI-SPEC states with correct copy.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` exits 0
- `pnpm test:run src/components/DropZone/DropZone.test.tsx` exits 0 with 6 passing tests
- `pnpm build` exits 0
- All UI-SPEC copy strings verbatim
</verification>

<success_criteria>
- INGEST-01 satisfied: drag-drop or click of a PDF file fires onFileAccepted; non-PDFs trigger onFileRejected with reason
- PrivacyBanner sets the privacy frame above-the-fold with the locked copy
- Design tokens and keyframes are now globally available for Plans 09 and 10
</success_criteria>

<output>
Create `.planning/phases/01-vertical-slice/01-08-SUMMARY.md` recording the DropZone props contract, the four state strings, and the new CSS tokens added.
</output>
