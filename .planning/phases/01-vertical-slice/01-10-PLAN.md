---
phase: 01-vertical-slice
plan: 10
type: execute
wave: 3
depends_on: ["08"]
files_modified:
  - src/components/GenerateLetterButton/GenerateLetterButton.tsx
  - src/components/GenerateLetterButton/GenerateLetterButton.module.css
  - src/components/GenerateLetterButton/GenerateLetterButton.test.tsx
  - src/components/ToastError/ToastError.tsx
  - src/components/ToastError/ToastError.module.css
autonomous: true
requirements:
  - LETTER-01

must_haves:
  truths:
    - "GenerateLetterButton renders a sticky-footer CTA when at least one flagged line exists"
    - "Disabled state copy: 'No overcharges to dispute.'"
    - "Active state copy: 'Generate dispute letter →' (with U+2192 right arrow)"
    - "Loading state copy: 'Building your letter…'"
    - "ToastError renders title + body, role=alert, auto-dismisses at 5s"
    - "GenerateLetterButton uses aria-disabled (not the disabled attribute) per UI-SPEC"
  artifacts:
    - path: "src/components/GenerateLetterButton/GenerateLetterButton.tsx"
      provides: "Sticky-footer CTA wired to caller-supplied onGenerate handler"
      exports: ["GenerateLetterButton", "GenerateLetterButtonProps"]
    - path: "src/components/ToastError/ToastError.tsx"
      provides: "Auto-dismissing toast for OCR/PDF/wrong-type errors"
      exports: ["ToastError", "ToastErrorProps"]
  key_links:
    - from: "src/components/GenerateLetterButton/GenerateLetterButton.tsx"
      to: "Plan 11 App handler"
      via: "onGenerate callback prop"
      pattern: "onGenerate"
    - from: "src/components/ToastError/ToastError.tsx"
      to: "auto-dismiss timer"
      via: "setTimeout 5000"
      pattern: "setTimeout"
---

<objective>
Build the bottom-of-page CTA (`GenerateLetterButton`, sticky footer per UI-SPEC.md section 6) and the error toast (`ToastError`, fixed position per UI-SPEC.md section 7). The button is the user's payoff: it triggers the pdf-lib generator from Plan 07 and downloads the dispute letter. The toast surfaces wrong-file-type, OCR failure, and PDF failure copy from UI-SPEC.md lines 511-518.

This plan does NOT wire the actual onGenerate handler — that is Plan 11 (App.tsx). This plan just builds the component shells with state-driven copy and clean callbacks.

Purpose: Satisfies LETTER-01 (UI surface). Plan 11 wires the onGenerate to a function that calls `generateDisputeLetter` and triggers the download.
Output: Two components + GenerateLetterButton tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-vertical-slice/01-UI-SPEC.md
@.planning/phases/01-vertical-slice/01-RESEARCH.md

<interfaces>
GenerateLetterButtonProps:
  flaggedCount: number
  isLoading: boolean
  onGenerate: () => void

If flaggedCount === 0 and isLoading === false: render the disabled state ("No overcharges to dispute.")
If flaggedCount > 0 and isLoading === false: render the active state ("Generate dispute letter →")
If isLoading === true: render the loading state ("Building your letter…") with spinner
If flaggedCount === 0 and the parent never mounts the button until line items exist (UI-SPEC.md line 350: "Footer is not rendered at all until at least one LineItemCard exists"). Plan 11 controls mounting; this component handles the within-mounted states.

ToastErrorProps:
  title: string
  body: string
  onDismiss: () => void

Auto-dismiss after 5000ms via useEffect + setTimeout. Manual dismiss via × button.

The right-arrow glyph in the active button copy is U+2192 RIGHT ARROW (literal `→` character). UI-SPEC.md line 24 confirms.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GenerateLetterButton component + tests</name>
  <files>src/components/GenerateLetterButton/GenerateLetterButton.tsx, src/components/GenerateLetterButton/GenerateLetterButton.module.css, src/components/GenerateLetterButton/GenerateLetterButton.test.tsx</files>
  <read_first>
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 328-371, "6. GenerateLetterButton")
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 502-509, GenerateLetterButton copy contract)
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-RESEARCH.md (lines 904-919, GenerateLetterButton tests)
  </read_first>
  <behavior>
    - Test 1 ("disabled when flaggedCount=0"): role=button, aria-disabled='true', text "No overcharges to dispute."
    - Test 2 ("active when flaggedCount>=1"): role=button, aria-disabled !== 'true', text contains "Generate dispute letter"
    - Test 3 ("loading"): text "Building your letter…", aria-disabled='true'
    - Test 4 ("calls onGenerate when active button clicked"): with flaggedCount=1, isLoading=false, click triggers the supplied onGenerate
    - Test 5 ("does NOT call onGenerate when disabled"): with flaggedCount=0, click does not trigger onGenerate
  </behavior>
  <action>
Create `src/components/GenerateLetterButton/GenerateLetterButton.tsx`:
- Imports: React, the CSS module.
- Exports `interface GenerateLetterButtonProps { flaggedCount: number; isLoading: boolean; onGenerate: () => void }`.
- Exports `function GenerateLetterButton(props)`.

Logic:
- `const isDisabled = props.isLoading || props.flaggedCount === 0`.
- Compute the displayed copy:
    - If `props.isLoading`: 'Building your letter…'
    - Else if `props.flaggedCount === 0`: 'No overcharges to dispute.'
    - Else: 'Generate dispute letter →' (literal U+2192 character).
- Render a `<footer className={styles.footer}>` containing a `<button>`:
  - className: `styles.button` plus conditional `styles.disabled` or `styles.loading`
  - role: omitted (native button)
  - aria-disabled: `isDisabled ? 'true' : 'false'`
  - aria-label: `isDisabled ? 'No overcharges found to dispute' : 'Generate dispute letter PDF and download'`
  - onClick: `() => { if (!isDisabled) props.onGenerate(); }`
  - When loading, render an inline spinner span left of the text (`<span className={styles.spinner} aria-hidden="true" />`).
  - Text content: the computed copy.

Use `aria-disabled` not the `disabled` attribute (UI-SPEC.md line 363 + RESEARCH.md Q12.5). Keep the element focusable so screen readers can read the disabled-state copy.

Create `src/components/GenerateLetterButton/GenerateLetterButton.module.css`:
- `.footer`: `position: sticky`, `bottom: 0`, `z-index: 50`, `width: 100%`, `background: var(--bg)`, `border-top: 1px solid var(--border)`, `padding: var(--space-md)`, `display: flex`, `justify-content: center`.
- `.button`: `width: 100%`, `max-width: 480px`, `height: 52px`, `border: none`, `border-radius: 8px`, `background: var(--accent)`, `color: #ffffff`, `font-family: var(--sans)`, `font-size: 16px`, `font-weight: 700`, `cursor: pointer`, `display: flex`, `align-items: center`, `justify-content: center`, `gap: var(--space-sm)`, `transition: filter 120ms ease, transform 80ms ease`.
- `.button:hover:not(.disabled):not(.loading)`: `filter: brightness(1.08)`.
- `.button:active:not(.disabled):not(.loading)`: `transform: scale(0.98)`.
- `.button:focus-visible`: `outline: 2px solid var(--accent)`, `outline-offset: 2px`.
- `.disabled`: `background: var(--surface)`, `color: var(--text)`, `cursor: not-allowed`, `opacity: 0.6`.
- `.loading`: `cursor: wait`, `opacity: 0.85`.
- `.spinner`: `width: 16px`, `height: 16px`, `border: 2px solid #ffffff66`, `border-top-color: #ffffff`, `border-radius: 50%`, `animation: spin 0.8s linear infinite`.

Create `src/components/GenerateLetterButton/GenerateLetterButton.test.tsx`. Use Testing Library with `@testing-library/user-event` for click realism. Each test renders fresh.

Test 1: render with `flaggedCount={0}`, `isLoading={false}`, `onGenerate={vi.fn()}`. `expect(screen.getByRole('button')).toHaveAttribute('aria-disabled', 'true')`. `expect(screen.getByRole('button')).toHaveTextContent('No overcharges to dispute.')`.

Test 2: render with `flaggedCount={1}`, `isLoading={false}`. `expect(screen.getByRole('button')).not.toHaveAttribute('aria-disabled', 'true')` (or `aria-disabled="false"`). `expect(screen.getByRole('button')).toHaveTextContent(/Generate dispute letter/)`.

Test 3: render with `isLoading={true}`, `flaggedCount={1}`. `expect(screen.getByRole('button')).toHaveTextContent('Building your letter…')`. `expect(screen.getByRole('button')).toHaveAttribute('aria-disabled', 'true')`.

Test 4: const onGenerate = vi.fn(); render with `flaggedCount={1}`, `isLoading={false}`, `onGenerate`. `await userEvent.click(screen.getByRole('button'))`. `expect(onGenerate).toHaveBeenCalledTimes(1)`.

Test 5: const onGenerate = vi.fn(); render with `flaggedCount={0}`, `isLoading={false}`, `onGenerate`. `await userEvent.click(screen.getByRole('button'))`. `expect(onGenerate).not.toHaveBeenCalled()`.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test:run src/components/GenerateLetterButton/GenerateLetterButton.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/components/GenerateLetterButton/GenerateLetterButton.tsx`
    - File exists: `src/components/GenerateLetterButton/GenerateLetterButton.module.css`
    - File exists: `src/components/GenerateLetterButton/GenerateLetterButton.test.tsx`
    - `grep "Generate dispute letter →" src/components/GenerateLetterButton/GenerateLetterButton.tsx` returns a match
    - `grep "No overcharges to dispute." src/components/GenerateLetterButton/GenerateLetterButton.tsx` returns a match
    - `grep "Building your letter" src/components/GenerateLetterButton/GenerateLetterButton.tsx` returns a match
    - `grep "aria-disabled" src/components/GenerateLetterButton/GenerateLetterButton.tsx` returns a match
    - `grep -E 'disabled=' src/components/GenerateLetterButton/GenerateLetterButton.tsx` returns NOTHING (must use aria-disabled, not the HTML disabled attribute)
    - All 5 tests pass under `pnpm test:run`
  </acceptance_criteria>
  <done>Sticky-footer CTA implemented with the four state-driven copy strings, correct ARIA, click handler that respects disabled state.</done>
</task>

<task type="auto">
  <name>Task 2: ToastError component</name>
  <files>src/components/ToastError/ToastError.tsx, src/components/ToastError/ToastError.module.css</files>
  <read_first>
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 373-409, "7. ToastError")
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 511-518, error states copy table)
  </read_first>
  <action>
Create `src/components/ToastError/ToastError.tsx`:
- Imports: React, useEffect, the CSS module.
- Exports `interface ToastErrorProps { title: string; body: string; onDismiss: () => void }`.
- Exports `function ToastError(props)`.
- useEffect: on mount, call `setTimeout(props.onDismiss, 5000)`. Return cleanup that clears the timeout.
- Render `<div role="alert" aria-live="assertive" aria-atomic="true" className={styles.toast}>`:
  - `<div className={styles.title}>{props.title}</div>`
  - `<div className={styles.body}>{props.body}</div>`
  - `<button type="button" className={styles.close} aria-label="Dismiss error" onClick={props.onDismiss}>×</button>` (literal U+00D7)

Create `src/components/ToastError/ToastError.module.css`:
- `.toast`: `position: fixed`, `bottom: var(--space-lg)`, `right: var(--space-lg)`, `min-width: 240px`, `max-width: 360px`, `padding: var(--space-md)`, `background: #1c1917`, `color: #fafaf9`, `border-radius: 8px`, `box-shadow: 0 10px 30px rgba(0,0,0,0.25)`, `z-index: 200`, `animation: toastIn 200ms ease-out`.
- `@media (max-width: 639px) { .toast { right: 50%; transform: translateX(50%); bottom: calc(84px + var(--space-md)); } }` (sits above the sticky footer on mobile per UI-SPEC.md line 380).
- `.title`: `font-size: 14px`, `font-weight: 700`, `color: #fafaf9`, `margin-bottom: var(--space-xs)`.
- `.body`: `font-size: 14px`, `font-weight: 400`, `color: #d6d3d1`.
- `.close`: `position: absolute`, `top: var(--space-xs)`, `right: var(--space-xs)`, `width: 32px`, `height: 32px`, `display: flex`, `align-items: center`, `justify-content: center`, `background: transparent`, `border: none`, `color: #a8a29e`, `font-size: 20px`, `cursor: pointer`.

Note: this plan does NOT include a unit test for ToastError. The auto-dismiss timer involves fake timers; testing it cleanly is straightforward but not on the critical path. Plan 11's integration smoke test exercises the toast indirectly when a wrong-type file is dropped.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/components/ToastError/ToastError.tsx`
    - File exists: `src/components/ToastError/ToastError.module.css`
    - `grep 'role="alert"' src/components/ToastError/ToastError.tsx` returns a match
    - `grep 'aria-live="assertive"' src/components/ToastError/ToastError.tsx` returns a match
    - `grep "setTimeout" src/components/ToastError/ToastError.tsx` returns a match
    - `grep "5000" src/components/ToastError/ToastError.tsx` returns a match
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>ToastError renders fixed-position error notice, auto-dismisses at 5s, manually dismissable.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` exits 0
- `pnpm test:run src/components/GenerateLetterButton/GenerateLetterButton.test.tsx` exits 0 with 5 passing tests
- `pnpm build` exits 0
- All UI-SPEC copy strings verbatim
</verification>

<success_criteria>
- LETTER-01 UI surface present: a sticky-footer CTA with the locked four-state copy and a hooked-up onGenerate prop
- Plan 11 (App integration) imports both components and supplies handlers wiring them to the audit state and error state
</success_criteria>

<output>
Create `.planning/phases/01-vertical-slice/01-10-SUMMARY.md` recording the GenerateLetterButton state machine (4 states), ToastError props contract, and the aria-disabled-not-disabled rationale.
</output>
