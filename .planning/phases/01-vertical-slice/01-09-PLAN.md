---
phase: 01-vertical-slice
plan: 09
type: execute
wave: 3
depends_on: ["06", "08"]
files_modified:
  - src/components/LineItemCard/LineItemCard.tsx
  - src/components/LineItemCard/LineItemCard.module.css
  - src/components/FlagBadge/FlagBadge.tsx
  - src/components/FlagBadge/FlagBadge.module.css
  - src/components/FlagBadge/FlagBadge.test.tsx
  - src/components/TraceDetail/TraceDetail.tsx
  - src/components/TraceDetail/TraceDetail.module.css
autonomous: true
requirements:
  - OCR-02

must_haves:
  truths:
    - "LineItemCard renders CPT code, description, and charge for unflagged items"
    - "Flagged LineItemCard renders the FlagBadge inline and is keyboard-expandable"
    - "FlagBadge shows the multiplier formatted as e.g. '4.0× hospital published rate'"
    - "TraceDetail renders the four data rows + plain-English sentence per UI-SPEC"
    - "TraceDetail uses the HTML hidden attribute (not display:none) when collapsed"
  artifacts:
    - path: "src/components/LineItemCard/LineItemCard.tsx"
      provides: "Card rendering one parsed bill line, expandable when flagged"
      exports: ["LineItemCard", "LineItemCardProps"]
    - path: "src/components/FlagBadge/FlagBadge.tsx"
      provides: "Inline pill badge announcing the multiplier"
      exports: ["FlagBadge"]
    - path: "src/components/TraceDetail/TraceDetail.tsx"
      provides: "Expanded rule-trace block under flagged cards"
      exports: ["TraceDetail"]
  key_links:
    - from: "src/components/LineItemCard/LineItemCard.tsx"
      to: "src/components/FlagBadge/FlagBadge.tsx"
      via: "child render when flag is present"
      pattern: "FlagBadge"
    - from: "src/components/LineItemCard/LineItemCard.tsx"
      to: "src/components/TraceDetail/TraceDetail.tsx"
      via: "child render when expanded"
      pattern: "TraceDetail"
---

<objective>
Build the three components that visualize a parsed bill row and its audit verdict: `LineItemCard` (the row container, click-to-expand when flagged), `FlagBadge` (inline pill announcing the multiplier — the "screenshot moment"), and `TraceDetail` (the four-line rule trace + plain-English explanation that appears when a flagged card is expanded).

Purpose: This is the demo's hero visual. CONTEXT.md "specifics" calls out optimizing for the moment the 99213 row gets the red `4.0× hospital published rate` badge. UI-SPEC.md sections 3-5 are the contract.
Output: Three components + FlagBadge tests. CSS modules for each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-vertical-slice/01-UI-SPEC.md
@.planning/phases/01-vertical-slice/01-RESEARCH.md

<interfaces>
LineItemCardProps:
  cptCode: string
  description: string
  charge: number
  flag: FlagResult | null
  isExpanded: boolean
  onToggleExpand: () => void

FlagBadgeProps:
  multiplier: number  // e.g., 4.0, 2.3

TraceDetailProps:
  cptCode: string
  flag: FlagResult           // contains charged, comparisonRate, multiplier, plainEnglishMultiplier
  isExpanded: boolean

FlagResult comes from src/lib/auditor/flagLine.ts (Plan 06).

The multiplier sign is U+00D7 MULTIPLICATION SIGN (×), NOT lowercase x — UI-SPEC.md line 487.

Plain-English sentence rules (UI-SPEC.md lines 499-500):
  - Whole multipliers: "We found you were charged FOUR times what this hospital publicly says they charge for this code." (FOUR comes from flag.plainEnglishMultiplier)
  - Fractional multipliers: "We found you were charged 2.3 times what this hospital publicly says they charge for this code."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FlagBadge component + tests</name>
  <files>src/components/FlagBadge/FlagBadge.tsx, src/components/FlagBadge/FlagBadge.module.css, src/components/FlagBadge/FlagBadge.test.tsx</files>
  <read_first>
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 261-285, "4. FlagBadge")
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 484-487, FlagBadge copy contract)
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-RESEARCH.md (lines 891-901, FlagBadge test pattern)
  </read_first>
  <behavior>
    - Test 1: renders text containing "4.0×" when multiplier=4.0
    - Test 2: renders text containing "2.3×" when multiplier=2.3
    - Test 3: aria-label format equals "Overcharge flag: 4.0 times hospital published rate"
    - Test 4: element has role="status"
  </behavior>
  <action>
Create `src/components/FlagBadge/FlagBadge.tsx`:
- Import the CSS module.
- Props: `{ multiplier: number }`.
- Format the displayed multiplier as `${multiplier.toFixed(1)}×` using U+00D7 (the literal character × in the source).
- Render `<span className={styles.badge} role="status" aria-label={\`Overcharge flag: ${multiplier.toFixed(1)} times hospital published rate\`}>{`${multiplier.toFixed(1)}× hospital published rate`}</span>`.

Create `src/components/FlagBadge/FlagBadge.module.css`:
- `.badge`: `display: inline-flex`, `align-items: center`, `padding: var(--space-xs) var(--space-sm)`, `border-radius: 4px`, `background: var(--flag-red)`, `color: var(--flag-red-text)`, `border: 1px solid var(--flag-red-border)`, `font-family: var(--mono)`, `font-size: 14px`, `font-weight: 700`, `line-height: 1`, `height: 24px`, `animation: badgeEntrance 200ms ease-out`.

Create `src/components/FlagBadge/FlagBadge.test.tsx`:
- Test 1 ("renders 4.0× formatted multiplier"): `render(<FlagBadge multiplier={4.0} />)`; assert `screen.getByText(/4\.0×/)` is present.
- Test 2 ("renders 2.3× for fractional multiplier"): `render(<FlagBadge multiplier={2.3} />)`; assert `screen.getByText(/2\.3×/)` is present.
- Test 3 ("aria-label format"): assert `screen.getByRole('status')` has attribute `aria-label="Overcharge flag: 4.0 times hospital published rate"`.
- Test 4 ("role status"): `expect(screen.getByRole('status')).toBeInTheDocument()`.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test:run src/components/FlagBadge/FlagBadge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/components/FlagBadge/FlagBadge.tsx`
    - File exists: `src/components/FlagBadge/FlagBadge.module.css`
    - File exists: `src/components/FlagBadge/FlagBadge.test.tsx`
    - `grep "role=\"status\"" src/components/FlagBadge/FlagBadge.tsx` returns a match
    - `grep "hospital published rate" src/components/FlagBadge/FlagBadge.tsx` returns a match
    - `grep "var(--flag-red)" src/components/FlagBadge/FlagBadge.module.css` returns a match
    - `grep "badgeEntrance" src/components/FlagBadge/FlagBadge.module.css` returns a match
    - All 4 tests pass under `pnpm test:run`
  </acceptance_criteria>
  <done>FlagBadge renders the screenshot-moment pill with correct copy, color, ARIA, and entrance animation.</done>
</task>

<task type="auto">
  <name>Task 2: TraceDetail component</name>
  <files>src/components/TraceDetail/TraceDetail.tsx, src/components/TraceDetail/TraceDetail.module.css</files>
  <read_first>
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 287-326, "5. TraceDetail")
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 489-500, TraceDetail copy contract)
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-RESEARCH.md (lines 1018-1033, accessibility hidden vs display:none)
  </read_first>
  <action>
Create `src/components/TraceDetail/TraceDetail.tsx`:
- Import the CSS module and the `FlagResult` type from `'../../lib/auditor/flagLine'`.
- Props: `{ cptCode: string; flag: FlagResult; isExpanded: boolean }`.
- Render a `<div>` with: `id={\`trace-${cptCode}\`}`, `role="region"`, `aria-label={\`Rule trace for CPT ${cptCode}\`}`, `hidden={!isExpanded}` (HTML attribute, not CSS — UI-SPEC.md line 316 + RESEARCH.md Q12.4).
- Inside, render the four mono-font rows in a `<dl>` or just `<div>` rows, each formatted exactly per UI-SPEC.md lines 491-495:
  - `Rule: charge > 1.5× hospital published rate (matched).`
  - `Charged: ${`$${flag.charged.toFixed(2)}`}`
  - `Hospital published rate: ${`$${flag.comparisonRate.toFixed(2)}`}`
  - `Multiplier: ${flag.multiplier.toFixed(1)}×`
- Below the rows, render the plain-English sentence (UI-SPEC.md line 497):
  - If `flag.plainEnglishMultiplier` is letters only (uppercase A-Z), render `<p>We found you were charged <strong>{flag.plainEnglishMultiplier}</strong> times what this hospital publicly says they charge for this code.</p>`
  - Otherwise render `<p>We found you were charged <strong>{flag.plainEnglishMultiplier}</strong> times what this hospital publicly says they charge for this code.</p>` with `flag.plainEnglishMultiplier` as the digit string (e.g., "2.3"). Both branches use the same JSX shape — only the strong content differs.

Create `src/components/TraceDetail/TraceDetail.module.css`:
- `.trace`: `background: var(--surface)`, `border-top: 1px solid var(--border)`, `border-radius: 0 0 8px 8px`, `padding: var(--space-md)`, `font-family: var(--mono)`, `font-size: 14px`, `color: var(--text)`.
- `.trace[hidden]`: `display: none` (the hidden attribute already implies this; explicit rule is a fallback).
- `.row + .row`: `margin-top: 4px`.
- `.row` font: mono 14px.
- `.plain`: `margin-top: var(--space-md)`, `font-family: var(--sans)`, `font-size: 16px`, `font-weight: 400`, `color: var(--text)`, `line-height: 1.5`.
- `.plain strong`: `font-weight: 700`, `color: var(--text-h)`.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/components/TraceDetail/TraceDetail.tsx`
    - File exists: `src/components/TraceDetail/TraceDetail.module.css`
    - `grep 'role="region"' src/components/TraceDetail/TraceDetail.tsx` returns a match
    - `grep "hidden={!isExpanded}" src/components/TraceDetail/TraceDetail.tsx` returns a match
    - `grep "charge > 1.5× hospital published rate (matched)" src/components/TraceDetail/TraceDetail.tsx` returns a match
    - `grep "We found you were charged" src/components/TraceDetail/TraceDetail.tsx` returns a match
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>TraceDetail renders the locked four-row data block + plain-English sentence; uses HTML hidden attribute for a11y.</done>
</task>

<task type="auto">
  <name>Task 3: LineItemCard component</name>
  <files>src/components/LineItemCard/LineItemCard.tsx, src/components/LineItemCard/LineItemCard.module.css</files>
  <read_first>
    - /Users/kevinrichards/Projects/hightimized/.planning/phases/01-vertical-slice/01-UI-SPEC.md (lines 218-259, "3. LineItemCard")
  </read_first>
  <action>
Create `src/components/LineItemCard/LineItemCard.tsx`:
- Import the CSS module, FlagBadge, TraceDetail, and the FlagResult type from auditor.
- Props:
    cptCode: string
    description: string
    charge: number
    flag: FlagResult | null
    isExpanded: boolean
    onToggleExpand: () => void
- If `flag` is null: render an `<article>` with className `styles.card`, no role, three children spans:
  - `<span className={styles.code}>{cptCode}</span>`
  - `<span className={styles.description}>{description}</span>`
  - `<span className={styles.amount}>${charge.toFixed(2)}</span>`
- If `flag` is present: render an `<article>` with className `\`${styles.card} ${styles.flagged}\``, `role="button"`, `tabIndex={0}`, `aria-expanded={isExpanded}`, `aria-controls={\`trace-${cptCode}\`}`, onClick handler that calls onToggleExpand, and onKeyDown handler that calls onToggleExpand when key is Enter or Space (preventDefault on Space).
  - Inside the card body row, the same three spans plus `<FlagBadge multiplier={flag.multiplier} />` after the description.
  - After the card body row, render `<TraceDetail cptCode={cptCode} flag={flag} isExpanded={isExpanded} />`.

Create `src/components/LineItemCard/LineItemCard.module.css`:
- `.card`: `display: flex`, `flex-wrap: wrap`, `align-items: center`, `gap: var(--space-sm)`, `width: 100%`, `padding: var(--space-md)`, `background: var(--bg)`, `border: 1px solid var(--border)`, `border-radius: 8px`, `min-height: 56px`, `font-family: var(--sans)`.
- `.flagged`: `border-left: 3px solid var(--flag-red)`, `background: #fff9f9`, `cursor: pointer`.
- `@media (prefers-color-scheme: dark) { .flagged { background: #1f1515; } }`.
- `.flagged:focus-visible`: `outline: 2px solid var(--accent)`, `outline-offset: 2px`.
- `.code`: `font-family: var(--mono)`, `font-size: 14px`, `font-weight: 700`, `color: var(--text-h)`, `min-width: 64px`.
- `.description`: `flex: 1 1 auto`, `font-size: 16px`, `color: var(--text-h)`, `font-weight: 400`.
- `.amount`: `font-family: var(--mono)`, `font-size: 14px`, `font-weight: 700`, `color: var(--text-h)`, `text-align: right`, `margin-left: auto`.
- `@media (max-width: 639px) { .description, .amount { flex-basis: 100%; } }`.

The card row container is a flex row that wraps. On desktop the three children + badge sit on one line; on mobile description and amount wrap to a second line. The TraceDetail is a sibling of that row (not a child of the flex row) so it doesn't get the row's flex layout — wrap each card in a `<div className={styles.cardWrapper}>` if needed, or simply put the row spans inside a child `<div className={styles.row}>` and TraceDetail outside it. Use the latter pattern: outer `<article>` is `display: block`; inner `.row` is `display: flex`.

Update CSS accordingly:
- `.card` becomes `display: block` (not flex).
- Add `.row`: the flex layout above.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `src/components/LineItemCard/LineItemCard.tsx`
    - File exists: `src/components/LineItemCard/LineItemCard.module.css`
    - `grep "FlagBadge" src/components/LineItemCard/LineItemCard.tsx` returns a match
    - `grep "TraceDetail" src/components/LineItemCard/LineItemCard.tsx` returns a match
    - `grep "aria-expanded" src/components/LineItemCard/LineItemCard.tsx` returns a match
    - `grep "aria-controls" src/components/LineItemCard/LineItemCard.tsx` returns a match
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>LineItemCard renders parsed line; when flagged, becomes a keyboard-operable expandable region wrapping FlagBadge + TraceDetail.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` exits 0
- `pnpm test:run src/components/FlagBadge/FlagBadge.test.tsx` exits 0 with 4 passing tests
- All copy strings match UI-SPEC.md verbatim
- TraceDetail uses HTML hidden attribute, not CSS display:none
</verification>

<success_criteria>
- The 99213 row at $750 visualizes correctly: red left border, FlagBadge inline showing "4.0× hospital published rate", clickable to reveal the four trace rows + "We found you were charged FOUR times..." sentence
- LineItemCard, FlagBadge, TraceDetail are decoupled — Plan 11 imports them and supplies props from the audit state
</success_criteria>

<output>
Create `.planning/phases/01-vertical-slice/01-09-SUMMARY.md` recording the props contracts for all three components and the wrapping pattern (article > row + TraceDetail) used to keep flex isolated from the trace block.
</output>
