---
phase: 00-decisions-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "pnpm dev starts a Vite dev server on http://localhost:5173/hightimized/ that returns 200"
    - "pnpm build emits a dist/ folder with asset URLs prefixed by /hightimized/"
    - "TypeScript strict mode is on and pnpm typecheck exits 0"
  artifacts:
    - path: "package.json"
      provides: "pnpm scripts (dev, build, preview, typecheck) + pinned deps"
      contains: '"vite"'
    - path: "vite.config.ts"
      provides: "GitHub Pages base subpath + React SWC plugin"
      contains: "base: '/hightimized/'"
    - path: "tsconfig.json"
      provides: "strict TypeScript config"
      contains: '"strict": true'
    - path: "src/main.tsx"
      provides: "React root mount"
      contains: "createRoot"
  key_links:
    - from: "vite.config.ts"
      to: "@vitejs/plugin-react-swc"
      via: "import + plugins array"
      pattern: "@vitejs/plugin-react-swc"
    - from: "index.html"
      to: "src/main.tsx"
      via: "<script type=\"module\" src=\"/src/main.tsx\">"
      pattern: "src/main.tsx"
---

<objective>
Initialize the Vite + TypeScript + React + pnpm project skeleton with the GitHub Pages subpath base configured. After this plan, `pnpm dev` runs and `pnpm build` emits assets with the correct `/hightimized/` prefix.

Purpose: Establishes the runtime foundation for every subsequent phase. The base path is locked here because changing it later breaks every absolute import.

Output: package.json, pnpm-lock.yaml, vite.config.ts, tsconfig pair, default React entry files, .gitignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-decisions-scaffolding/00-CONTEXT.md
@.planning/phases/00-decisions-scaffolding/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite react-ts and swap to SWC</name>
  <files>package.json, pnpm-lock.yaml, index.html, src/main.tsx, src/App.tsx, src/vite-env.d.ts, .gitignore, public/vite.svg</files>
  <read_first>
    - .planning/phases/00-decisions-scaffolding/00-RESEARCH.md (sections "Standard Stack" and "Pitfall 1")
    - .planning/phases/00-decisions-scaffolding/00-CONTEXT.md (decisions: Stack Choices)
  </read_first>
  <action>
Working directory: /Users/kevinrichards/Projects/hightimized/

The directory currently contains only `.planning/`, `CLAUDE.md`, `.git/`, and `.code-review-graph/`. Scaffold a Vite project IN PLACE.

1. Run: `pnpm create vite@latest . --template react-ts` and answer "y" to "Current directory is not empty. Continue?" — accept the merge. Do NOT use `--template react-swc-ts` (removed in Vite 6+ per Pitfall 1 in RESEARCH.md).

2. Run: `pnpm install` to install initial deps.

3. Replace the Babel React plugin with the SWC plugin (faster dev server, per RESEARCH.md Standard Stack):
   ```
   pnpm remove @vitejs/plugin-react
   pnpm add -D @vitejs/plugin-react-swc@4.3.0
   ```

4. Pin the core deps to RESEARCH.md versions. Edit package.json so the dependencies and devDependencies blocks read EXACTLY:
   ```json
   "dependencies": {
     "react": "19.2.5",
     "react-dom": "19.2.5"
   },
   "devDependencies": {
     "@types/react": "^19.2.0",
     "@types/react-dom": "^19.2.0",
     "@vitejs/plugin-react-swc": "4.3.0",
     "typescript": "6.0.3",
     "vite": "8.0.10"
   }
   ```
   Then run `pnpm install` again to refresh the lockfile.

5. Replace `.gitignore` with the canonical version from RESEARCH.md "Code Examples" → ".gitignore (Canonical for Vite + pnpm)" — copy that block VERBATIM. It must include `node_modules`, `dist`, `dist-ssr`, `.vite`, `data/build/`, `coverage`, `*.lcov`, `*.log`, `npm-debug.log*`, `pnpm-debug.log*`, `.env`, `.env.*`, `!.env.example`, `.vscode`, `.idea`, `*.swp`, `*.swo`, `.DS_Store`, `Thumbs.db`, `*.tsbuildinfo`.
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/hightimized && test -f package.json && test -f index.html && test -f src/main.tsx && grep -q '"vite": "8.0.10"' package.json && grep -q '@vitejs/plugin-react-swc' package.json && grep -q '"react": "19.2.5"' package.json && grep -q "data/build/" .gitignore</automated>
  </verify>
  <done>package.json has the exact pinned versions; .gitignore matches RESEARCH.md verbatim; src/main.tsx and src/App.tsx exist from the template.</done>
</task>

<task type="auto">
  <name>Task 2: Write vite.config.ts with GitHub Pages base + tsconfig pair</name>
  <files>vite.config.ts, tsconfig.json, tsconfig.node.json, package.json</files>
  <read_first>
    - vite.config.ts (whatever the scaffold produced)
    - tsconfig.json (whatever the scaffold produced)
    - .planning/phases/00-decisions-scaffolding/00-RESEARCH.md (sections "Pattern 1: Vite Config with GitHub Pages Base", "Pattern 5: TypeScript Config", "Pattern 6: package.json Scripts", "Pitfall 2")
  </read_first>
  <action>
1. Overwrite `vite.config.ts` with EXACTLY this content (the PWA plugin import is intentionally OMITTED here — Plan 04 adds it; this plan keeps the config minimal so dev/build work without vite-plugin-pwa installed yet):

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react-swc'

// base must match the GitHub Pages subpath (ByteWorthyLLC/hightimized).
// Phase 6 may switch to '/' if a custom domain is configured.
export default defineConfig({
  base: '/hightimized/',
  plugins: [react()],
})
```

2. Overwrite `tsconfig.json` with EXACTLY the block from RESEARCH.md "Pattern 5: TypeScript Config" (the `tsconfig.json — app code` block). Copy verbatim.

3. Overwrite `tsconfig.node.json` with EXACTLY the block from RESEARCH.md "Pattern 5: TypeScript Config" (the `tsconfig.node.json` block). Copy verbatim.

4. Edit package.json `"scripts"` block to match EXACTLY (drop "postinstall" for now — Plan 02 adds lefthook install):

```json
"scripts": {
  "dev": "vite",
  "build": "tsc -b && vite build",
  "preview": "vite preview",
  "typecheck": "tsc --noEmit"
}
```
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/hightimized && grep -q "base: '/hightimized/'" vite.config.ts && grep -q '@vitejs/plugin-react-swc' vite.config.ts && grep -q '"strict": true' tsconfig.json && grep -q '"jsx": "react-jsx"' tsconfig.json && pnpm typecheck</automated>
  </verify>
  <done>`pnpm typecheck` exits 0; vite.config.ts has the SWC plugin and base subpath; tsconfig.json is strict.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke test dev + build</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - package.json
    - vite.config.ts
  </read_first>
  <action>
1. Run `pnpm build` and confirm it exits 0.

2. Inspect the build output: `grep -r 'src=' dist/index.html | head -5` — every asset reference must start with `/hightimized/` (per Pitfall 2 in RESEARCH.md). If any asset path begins with `/assets/` (no `hightimized` prefix), the base config is wrong — STOP and fix.

3. Start dev server in the background: `pnpm dev &` then wait 3 seconds.

4. Verify dev server responds: `curl -sf -o /dev/null -w "%{http_code}" http://localhost:5173/hightimized/` must print `200`.

5. Kill the dev server: `pkill -f "vite"` (or kill by PID).

6. Delete `dist/` to keep the working tree clean for the next plan: `rm -rf dist`.
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/hightimized && pnpm build && grep -q '/hightimized/assets/' dist/index.html && rm -rf dist</automated>
  </verify>
  <done>pnpm build emits dist/index.html with /hightimized/-prefixed asset URLs; dev server returns 200 on /hightimized/.</done>
</task>

</tasks>

<verification>
After all tasks:
- `pnpm typecheck` exits 0
- `pnpm build` exits 0 and emits dist/ with /hightimized/-prefixed asset URLs
- `pnpm dev` serves http://localhost:5173/hightimized/ with HTTP 200
- package.json contains exactly the pinned versions from RESEARCH.md
</verification>

<success_criteria>
- The Vite + TS + React + SWC scaffold is in place
- vite.config.ts has the GitHub Pages base subpath set correctly
- TypeScript strict mode is enabled
- The default scaffold landing page renders in dev mode
</success_criteria>

<output>
After completion, create `.planning/phases/00-decisions-scaffolding/00-01-SUMMARY.md`
</output>
