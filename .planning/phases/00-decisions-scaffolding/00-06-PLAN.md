---
phase: 00-decisions-scaffolding
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - LICENSE
  - README.md
  - CONTRIBUTING.md
  - SECURITY.md
  - CODE_OF_CONDUCT.md
  - CHANGELOG.md
  - docs/decisions/0001-phase-0-decisions.md
  - .github/ISSUE_TEMPLATE/bug_report.md
  - .github/ISSUE_TEMPLATE/feature_request.md
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "LICENSE file at repo root contains MIT text with 'Copyright (c) 2026 ByteWorthy LLC'"
    - "README.md first heading is the thesis line, not the project name"
    - "ADR captures all 12 Phase 0 decisions in Nygard format at docs/decisions/0001-phase-0-decisions.md"
    - "README contains zero RCM/denial/prior-auth language (Upstream anti-overlap audit)"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license, ByteWorthy LLC copyright"
      contains: "MIT License"
    - path: "README.md"
      provides: "Marketing-grade skeleton with thesis hero + structure for Phase 6 assets"
      contains: "They charged you high. Get it itemized."
    - path: "docs/decisions/0001-phase-0-decisions.md"
      provides: "ADR with all 12 Phase 0 decisions"
      contains: "ADR 0001"
    - path: "CONTRIBUTING.md"
      provides: "Fork + PR instructions"
    - path: "SECURITY.md"
      provides: "Browser-only attack surface statement + GitHub Security Advisories link"
    - path: "CHANGELOG.md"
      provides: "Keep a Changelog stub with 0.0.1 Initial scaffold entry"
  key_links:
    - from: "README.md"
      to: "https://byteworthyllc.github.io/hightimized"
      via: "Try it now CTA"
      pattern: "byteworthyllc.github.io/hightimized"
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "contributor section link"
      pattern: "CONTRIBUTING.md"
---

<objective>
Write all OSS-credibility documentation: LICENSE, README skeleton (with marketing hero), ADR with all 12 decisions, CONTRIBUTING, SECURITY, CHANGELOG, CODE_OF_CONDUCT, and the .github issue + PR templates.

Purpose: Phase 6 fills demo video + hero GIF. Phase 0 must commit the structure + ADR so the public-launch credibility files exist from commit #1. The README MUST avoid Upstream Intelligence overlap language — this is a hard rule.

Output: 10 markdown files, all committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-decisions-scaffolding/00-CONTEXT.md
@.planning/phases/00-decisions-scaffolding/00-RESEARCH.md
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write LICENSE + ADR + CHANGELOG</name>
  <files>LICENSE, docs/decisions/0001-phase-0-decisions.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/00-decisions-scaffolding/00-RESEARCH.md (sections "MIT LICENSE", "ADR Format")
    - .planning/phases/00-decisions-scaffolding/00-CONTEXT.md (the 12 decisions)
  </read_first>
  <action>
1. Create `LICENSE` at the repo root with EXACTLY the MIT text from RESEARCH.md "MIT LICENSE" section. Copy verbatim. Copyright line MUST read: `Copyright (c) 2026 ByteWorthy LLC`.

2. Create `docs/decisions/` directory if missing: `mkdir -p docs/decisions`

3. Create `docs/decisions/0001-phase-0-decisions.md` with EXACTLY the content from RESEARCH.md "ADR Format (for docs/decisions/0001-phase-0-decisions.md)" section. Copy the entire markdown block verbatim — title, status, date, deciders, context, decisions table (all 12 rows), consequences. Do NOT abbreviate; do NOT omit any of the 12 decisions.

4. Create `CHANGELOG.md` with EXACTLY this content (Keep a Changelog format):

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.0.1] - 2026-04-28

### Added
- Initial Vite + TypeScript + React + pnpm scaffold
- ESLint flat config + Prettier + Lefthook + Vitest tooling layer
- vite-plugin-pwa stub configuration
- GitHub Actions CI + Pages deploy workflows
- ADR 0001 capturing Phase 0 architectural decisions
- README skeleton, LICENSE (MIT), CONTRIBUTING, SECURITY, CODE_OF_CONDUCT
```
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/hightimized && test -f LICENSE && grep -q "MIT License" LICENSE && grep -q "Copyright (c) 2026 ByteWorthy LLC" LICENSE && test -f docs/decisions/0001-phase-0-decisions.md && grep -q "ADR 0001" docs/decisions/0001-phase-0-decisions.md && grep -c "^| " docs/decisions/0001-phase-0-decisions.md | awk '$1 >= 13 {exit 0} {exit 1}' && test -f CHANGELOG.md && grep -q "0.0.1" CHANGELOG.md</automated>
  </verify>
  <done>LICENSE has MIT text + ByteWorthy LLC; ADR has all 12 decisions in a table; CHANGELOG.md has 0.0.1 Initial scaffold entry.</done>
</task>

<task type="auto">
  <name>Task 2: Write README + CONTRIBUTING + SECURITY + CODE_OF_CONDUCT</name>
  <files>README.md, CONTRIBUTING.md, SECURITY.md, CODE_OF_CONDUCT.md</files>
  <read_first>
    - .planning/phases/00-decisions-scaffolding/00-RESEARCH.md (sections "README Structure (Phase 0 Skeleton)", "OSS Credibility Files: Phase 0 vs Deferred", "SECURITY.md")
    - .planning/STATE.md (marketability + cost posture)
  </read_first>
  <action>
1. Create `README.md` with EXACTLY the content from RESEARCH.md "README Structure (Phase 0 Skeleton)" section — copy the markdown block verbatim. Critical rules per CONTEXT.md and STATE.md:
   - First heading is the thesis line `# They charged you high. Get it itemized.` — NOT `# hightimized`
   - The `[DEMO VIDEO PLACEHOLDER]` and `[HERO GIF]` HTML comments stay in — Phase 6 fills them
   - Zero emojis. Zero rocket-emoji marketing language. Zero "🚀 lightning fast." Zero "amazing."
   - Zero RCM, denial, prior-auth, or Upstream Intelligence overlap words. Audit the final text by running `grep -iE "(denial|prior auth|RCM|revenue cycle|claims management|appeals)" README.md` — must return zero matches.

2. Create `CONTRIBUTING.md` with EXACTLY this content:

```markdown
# Contributing to hightimized

Thanks for your interest. hightimized is MIT-licensed and built for forks.

## Quick start

```bash
pnpm install
pnpm dev
```

This wires the Lefthook pre-commit hooks automatically. Your commits will run typecheck, lint, prettier, and changed-file tests before they're accepted.

## Commit format

```
type(scope): message
```

Types: `feat`, `fix`, `chore`, `refactor`, `test`, `docs`, `perf`.

## Pull requests

1. Fork the repo
2. Create a branch: `feature/short-desc` or `fix/short-desc`
3. Run `pnpm test:run` and `pnpm lint` before pushing
4. Open a PR against `main`

## What's in scope

Patient-side bill audit, dispute letter generation, statute viewing, browser-only privacy. Anything that defeats the privacy hook (cloud sync, accounts, telemetry) is out of scope by design.

## What's out of scope

- Insurance-side workflows (denials, prior auth, appeals) — different project, different repo
- Cloud sync of bill data — defeats the privacy promise
- Negotiation-on-your-behalf services — out of scope by design

## Questions

Open a [Discussion](https://github.com/ByteWorthyLLC/hightimized/discussions) before opening an issue if you're unsure.
```

3. Create `SECURITY.md` with EXACTLY the content from RESEARCH.md "Minimal SECURITY.md for Phase 0" block. Copy verbatim.

4. Create `CODE_OF_CONDUCT.md` with EXACTLY this content (Contributor Covenant 2.1, abbreviated to the headline section — full text adopted by reference):

```markdown
# Code of Conduct

This project adopts the [Contributor Covenant 2.1](https://www.contributor-covenant.org/version/2/1/code_of_conduct/).

## Our pledge

We pledge to make participation in this project a harassment-free experience for everyone, regardless of age, body size, visible or invisible disability, ethnicity, sex characteristics, gender identity and expression, level of experience, education, socio-economic status, nationality, personal appearance, race, religion, or sexual identity and orientation.

## Reporting

Report unacceptable behavior to the project maintainers via [GitHub Security Advisories](https://github.com/ByteWorthyLLC/hightimized/security/advisories/new) (private channel). All complaints will be reviewed and investigated promptly and fairly.

## Enforcement

Project maintainers are responsible for clarifying and enforcing standards of acceptable behavior and will take appropriate and fair corrective action in response to any behavior that they deem inappropriate, threatening, offensive, or harmful.

Full text: https://www.contributor-covenant.org/version/2/1/code_of_conduct/
```
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/hightimized && test -f README.md && head -1 README.md | grep -q "They charged you high. Get it itemized." && ! grep -iqE "(denial|prior auth|RCM|revenue cycle|claims management|appeals)" README.md && test -f CONTRIBUTING.md && test -f SECURITY.md && grep -q "GitHub Security Advisories" SECURITY.md && test -f CODE_OF_CONDUCT.md</automated>
  </verify>
  <done>README first heading is thesis line; zero overlap-language matches; all 4 files exist with the spec content.</done>
</task>

<task type="auto">
  <name>Task 3: Write .github issue + PR templates</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.md, .github/ISSUE_TEMPLATE/feature_request.md, .github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - .planning/phases/00-decisions-scaffolding/00-RESEARCH.md (section "OSS Credibility Files")
  </read_first>
  <action>
1. Create `.github/ISSUE_TEMPLATE/` if missing: `mkdir -p .github/ISSUE_TEMPLATE`

2. Create `.github/ISSUE_TEMPLATE/bug_report.md` with EXACTLY this content:

```markdown
---
name: Bug report
about: Something doesn't work as expected
labels: bug
---

## What happened

A clear description of the bug.

## Steps to reproduce

1. Go to '...'
2. Drag in '...'
3. Click '...'
4. See error

## Expected behavior

What you expected to happen.

## Browser + OS

- Browser: (e.g. Chrome 138 / Firefox 142 / Safari 19)
- OS: (e.g. macOS 26 / Windows 12 / Ubuntu 26.04)
- WebGPU available: yes / no / don't know

## Privacy reminder

Do NOT paste actual bill text or personal data into this issue. The repo is public.
```

3. Create `.github/ISSUE_TEMPLATE/feature_request.md` with EXACTLY this content:

```markdown
---
name: Feature request
about: Suggest something hightimized should do
labels: enhancement
---

## What problem does this solve

The patient experience this would improve.

## Proposed solution

How it might work.

## Alternatives considered

What else you thought about.

## Scope check

- [ ] This stays browser-only (no server, no API call)
- [ ] This doesn't require an account
- [ ] This is patient-side, not insurance-side workflow
```

4. Create `.github/PULL_REQUEST_TEMPLATE.md` with EXACTLY this content:

```markdown
## What this changes

A short description of the change.

## Why

The problem this solves or the value it adds.

## How tested

- [ ] `pnpm test:run` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm build` passes
- [ ] Manually verified in `pnpm dev`

## Privacy + scope check

- [ ] No new network calls added
- [ ] No telemetry or analytics added
- [ ] No account/login system added
- [ ] No insurance-side workflow code added
```
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/hightimized && test -f .github/ISSUE_TEMPLATE/bug_report.md && test -f .github/ISSUE_TEMPLATE/feature_request.md && test -f .github/PULL_REQUEST_TEMPLATE.md && grep -q "WebGPU" .github/ISSUE_TEMPLATE/bug_report.md && grep -q "browser-only" .github/ISSUE_TEMPLATE/feature_request.md</automated>
  </verify>
  <done>Three .github templates exist with the spec content; bug report includes WebGPU + privacy reminder.</done>
</task>

</tasks>

<verification>
- All 10 doc files exist
- README has thesis line as first heading
- README contains zero RCM/denial/prior-auth/appeals language (anti-overlap audit)
- ADR captures all 12 Phase 0 decisions
- LICENSE has MIT text with ByteWorthy LLC copyright
</verification>

<success_criteria>
- OSS credibility files all in place
- ADR resolves Phase 0 decisions for posterity
- README skeleton ready for Phase 6 to fill demo assets
- Anti-overlap rule enforced (no Upstream Intelligence language anywhere)
</success_criteria>

<output>
After completion, create `.planning/phases/00-decisions-scaffolding/00-06-SUMMARY.md`
</output>
