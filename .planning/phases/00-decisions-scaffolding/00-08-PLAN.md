---
phase: 00-decisions-scaffolding
plan: 08
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - (no local files — external GitHub state changes only)
autonomous: false
requirements: []

must_haves:
  truths:
    - "The repo exists at github.com/ByteWorthyLLC/hightimized"
    - "The first commit (chore: initial scaffold + Phase 0 decisions) has been pushed to origin/main"
    - "GitHub Discussions is enabled on the repo"
    - "GitHub Pages source is set to 'GitHub Actions' (not branch deploy)"
    - "The CI workflow run for the first push completes green"
  artifacts:
    - path: "(remote) github.com/ByteWorthyLLC/hightimized"
      provides: "Public-or-private remote repo with first commit"
  key_links:
    - from: "local main"
      to: "origin/main"
      via: "git push -u origin main"
      pattern: "origin/main"
---

<objective>
Execute the gated external GitHub actions: create the repo at `github.com/ByteWorthyLLC/hightimized` (private initially), push the first commit, enable Discussions, and configure Pages source as "GitHub Actions". Each step pauses for explicit user authorization before running.

Purpose: STATE.md asks for the GitHub repo to be created during Phase 0. The user has separately directed that any actual `gh repo create`, `git push origin main`, or "enable Discussions" command must HALT for explicit user authorization. This plan honors that gate.

Output: Live remote repo, first commit visible on GitHub, Discussions enabled, Pages source configured.

This plan is `autonomous: false` so execute-phase pauses on every checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-decisions-scaffolding/00-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 1: Confirm ByteWorthyLLC org exists + user authorizes repo creation</name>
  <what-built>
    Local scaffold from Plans 01-07 is complete: pnpm dev works, build passes, first commit is sitting on local main, ready to push. Plan 08 is about to perform the first action that mutates external GitHub state.
  </what-built>
  <how-to-verify>
1. Confirm the GitHub org exists: visit `https://github.com/ByteWorthyLLC` in a browser. If 404, the org needs to be created first (this is org-admin work, not something `gh` can do for an arbitrary slug — abort if missing).

2. Confirm `gh` CLI is authenticated against an account that has create-repo rights on ByteWorthyLLC:
```bash
gh auth status
gh api orgs/ByteWorthyLLC -q .login 2>&1 | head -3
```

3. Confirm no repo named `hightimized` already exists in the org:
```bash
gh repo view ByteWorthyLLC/hightimized 2>&1 | head -3
```
Expected: a "404" or "Could not resolve" error. If the repo already exists, STOP and decide whether to push to it or rename — that is a user decision.

4. **Authorize**: type `approve` to authorize Tasks 2-5 (repo creation, push, Discussions, Pages config). Type `abort` or describe the issue to halt.
  </how-to-verify>
  <resume-signal>Type "approve" to proceed with all subsequent gated tasks. Type any other response to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create the GitHub repo (private)</name>
  <files>(no local files — remote state)</files>
  <read_first>
    - .planning/STATE.md (cost constraint — repo must be public eventually for Pages free tier; created private to stage README + LICENSE first)
  </read_first>
  <action>
Create the repo as PRIVATE first. CONTEXT.md specifies "private until first README/LICENSE land, then public" — we already have those locally and are about to push them, but we still create private to keep the door open for any final pre-push edits the user wants.

```bash
cd /Users/kevinrichards/Projects/hightimized

gh repo create ByteWorthyLLC/hightimized \
  --private \
  --description "Audit your hospital bill. Generate a dispute letter. Free, private, browser-only." \
  --source=. \
  --remote=origin
```

Note: `--source=.` registers the local working tree as the source and adds the remote `origin`. It does NOT push automatically. `--remote=origin` names the remote.

Verify:
```bash
git remote -v
gh repo view ByteWorthyLLC/hightimized -q .url
```

If `gh auth` lacks the right scope, the command will print an authorization gate URL — open it, authorize, retry. This is an authentication gate, not a planning failure.
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/hightimized && git remote -v | grep -q "ByteWorthyLLC/hightimized" && gh repo view ByteWorthyLLC/hightimized -q .name | grep -q "^hightimized$"</automated>
  </verify>
  <done>Repo exists at github.com/ByteWorthyLLC/hightimized (private); local origin remote points to it.</done>
</task>

<task type="auto">
  <name>Task 3: Push the first commit</name>
  <files>(no local files — remote state)</files>
  <read_first>
    - (Task 2 output — confirmed origin remote exists)
  </read_first>
  <action>
Push the local `main` branch to origin and set upstream:

```bash
cd /Users/kevinrichards/Projects/hightimized
git push -u origin main
```

Then verify the push landed:
```bash
gh repo view ByteWorthyLLC/hightimized -q .defaultBranchRef.name
git log -1 --pretty=format:"%H %s" | head -1
gh api repos/ByteWorthyLLC/hightimized/commits/main -q .sha
```

The local HEAD SHA and remote main SHA must match. The default branch should be `main`.

This push will trigger BOTH workflows from Plan 05 (ci.yml and deploy-pages.yml). The CI workflow will run on PR + push. The deploy-pages workflow will run on push to main but the deploy step will FAIL until Task 5 sets the Pages source — that is expected. The build step in deploy-pages will succeed.
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/hightimized && LOCAL=$(git rev-parse HEAD) && REMOTE=$(gh api repos/ByteWorthyLLC/hightimized/commits/main -q .sha) && [ "$LOCAL" = "$REMOTE" ]</automated>
  </verify>
  <done>Local main and origin/main are at the same commit SHA.</done>
</task>

<task type="auto">
  <name>Task 4: Enable Discussions on the repo</name>
  <files>(no local files — remote state)</files>
  <read_first>
    - .planning/STATE.md (supplemental user intent #2 — "open Discussions")
  </read_first>
  <action>
Enable Discussions via the GitHub API:

```bash
gh api -X PATCH repos/ByteWorthyLLC/hightimized -f has_discussions=true
```

Verify:
```bash
gh repo view ByteWorthyLLC/hightimized --json hasDiscussionsEnabled -q .hasDiscussionsEnabled
```

Must print `true`.
  </action>
  <verify>
    <automated>gh repo view ByteWorthyLLC/hightimized --json hasDiscussionsEnabled -q .hasDiscussionsEnabled | grep -q "^true$"</automated>
  </verify>
  <done>Discussions enabled on the repo.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Set GitHub Pages source to "GitHub Actions"</name>
  <what-built>Repo is live, first commit pushed, Discussions on. The remaining gated step is configuring Pages source. The GitHub web UI does not have a stable `gh` API for "set Pages source to GitHub Actions" that works without preview headers, so this step is human-clicked.</what-built>
  <how-to-verify>
1. Open: `https://github.com/ByteWorthyLLC/hightimized/settings/pages`

2. Under "Build and deployment" → "Source", select **"GitHub Actions"** (not "Deploy from a branch").

3. Save (the page auto-saves on selection).

4. Manually re-run the most recent failed deploy-pages workflow run:
   - Go to `https://github.com/ByteWorthyLLC/hightimized/actions/workflows/deploy-pages.yml`
   - Click the most recent run
   - Click "Re-run all jobs"

5. Wait ~2 minutes. The workflow should complete green and the site should be live at `https://byteworthyllc.github.io/hightimized/`.

6. Verify the site loads:
   ```bash
   curl -sf -o /dev/null -w "%{http_code}" https://byteworthyllc.github.io/hightimized/
   ```
   Should print `200`.

7. Type the result: `live` if the site loads, or describe the failure mode.
  </how-to-verify>
  <resume-signal>Type "live" once https://byteworthyllc.github.io/hightimized/ returns 200, or describe what failed.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Decide whether to flip repo from private to public</name>
  <decision>The repo is currently private. Phase 6 makes it public for launch. Decide now whether to flip it public immediately (now that README + LICENSE are landed) or keep private until Phase 6.</decision>
  <context>
CONTEXT.md: "private until first README/LICENSE land, then public." Both files are now in main. STATE.md cost constraint requires public-repo tier for free GitHub Pages + free unlimited Actions minutes.

Important: GitHub Pages on the *free tier for private repos* is now GitHub Pages-light (different limits). For free, unmetered Pages + Actions, the repo must be public.

Two options:
- **Option A: Flip public now.** Free Pages + Actions tier, demo URL is shareable, but the repo is discoverable before Phase 6 is ready. Risk: someone finds it before launch and shares half-baked screenshots.
- **Option B: Keep private until Phase 6.** Pages may be limited (free for public is the unmetered path). Phase 6 will explicitly flip public as part of v0.1.0 release.
  </context>
  <options>
    <option id="option-a">
      <name>Flip public now</name>
      <pros>Free Pages + Actions tier active immediately; matches the spirit of OSS thesis; CI minutes don't count against private-repo budget</pros>
      <cons>Repo discoverable pre-launch; pre-launch screenshots could leak</cons>
    </option>
    <option id="option-b">
      <name>Keep private until Phase 6</name>
      <pros>Clean Phase 6 launch moment; no pre-launch leaks</pros>
      <cons>Free Pages may have rate limits on private; private-repo Actions minutes are billed past free tier</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or describe a different choice.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Apply public/private decision (conditional)</name>
  <files>(no local files — remote state)</files>
  <read_first>
    - (decision from previous checkpoint)
  </read_first>
  <action>
Based on the decision from the previous checkpoint:

- If user chose **option-a (flip public now)**:
  ```bash
  gh repo edit ByteWorthyLLC/hightimized --visibility public --accept-visibility-change-consequences
  gh repo view ByteWorthyLLC/hightimized --json visibility -q .visibility
  ```
  Must print `PUBLIC`.

- If user chose **option-b (keep private)**: skip this task. Note in the SUMMARY that visibility flip is deferred to Phase 6.

- If user chose something else: follow user's instruction, document in SUMMARY.
  </action>
  <verify>
    <automated>gh repo view ByteWorthyLLC/hightimized --json visibility -q .visibility | grep -qE "^(PUBLIC|PRIVATE)$"</automated>
  </verify>
  <done>Repo visibility matches user's chosen option; SUMMARY.md notes which option was chosen.</done>
</task>

</tasks>

<verification>
- Repo exists at github.com/ByteWorthyLLC/hightimized
- First commit pushed to origin/main
- Discussions enabled
- Pages source set to "GitHub Actions"
- Site reachable at https://byteworthyllc.github.io/hightimized/
- Visibility is intentional (per user's decision)
</verification>

<success_criteria>
- All Phase 0 success criteria from ROADMAP.md are met:
  1. ADR committed at docs/decisions/0001-phase-0-decisions.md ✓ (Plan 06)
  2. Repo at ByteWorthyLLC/hightimized with MIT LICENSE ✓ (this plan + Plan 06)
  3. Vite + TypeScript + React scaffold ✓ (Plan 01)
  4. Lefthook + ESLint + Prettier + Vitest ✓ (Plans 02-03)
  5. GitHub Actions CI green on first commit ✓ (this plan + Plan 05)
  6. README has thesis line ✓ (Plan 06)
- Phase 0 ends; Phase 1 (Vertical Slice) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/00-decisions-scaffolding/00-08-SUMMARY.md`
</output>
